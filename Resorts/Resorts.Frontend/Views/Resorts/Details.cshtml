@model Resorts.Frontend.Models.ResortInfo

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ResortInfo</h4>
    <hr />
    <dl class="row">
        @Html.HiddenFor(model => model.ResortId)

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ResortName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ResortName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
    <div>
        @foreach (var img in Model.Images)
        {
            <span>
                <img src="@img">
            </span>
        }

    </div>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ResortId }) |
    <a asp-action="Index">Back to List</a>
</div>
